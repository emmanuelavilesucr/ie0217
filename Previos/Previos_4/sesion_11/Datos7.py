fruits = {"Apple", "Peach", "Mango"}   # Define un conjunto de frutas

# Itera sobre el conjunto y muestra el nombre de cada fruta
for fruit in fruits:
    print(fruit)
    
    
# Por otro lado, aqui se define un conjunto de numeros pares

event_numbers = {2, 4, 6, 8}

# Muestra el conjunto de numeros pares e indica la cantidad de elementos en el conjunto
print("Set:", event_numbers)
print("Total Elements:", len(event_numbers))